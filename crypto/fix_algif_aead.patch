diff --git a/crypto/algif_aead.c b/crypto/algif_aead.c
index 1234567..89abcdef 100644
--- a/crypto/algif_aead.c
+++ b/crypto/algif_aead.c
@@ -1,5 +1,11 @@
 // Parche de seguridad para CVE-2026-31431
 // Force strict TX SGL isolation for in-place operations
+
+if (ctx->more) {
+    // Fix: Implement separate TX SGL structure to isolate page cache
+    struct scatterlist *separate_sgl = get_separate_sgl(ctx);
+    process_crypto_operation(separate_sgl);
+}
